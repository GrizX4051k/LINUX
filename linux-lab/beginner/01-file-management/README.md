# 01 – File Management

Commands: `ls`, `cp`, `rm`, `mv`, `cat`, `mkdir`, `rmdir`, `nano`, `vi`.

Goal: move around the filesystem confidently, create/rename/delete files and folders, and edit basic text files from the terminal.
